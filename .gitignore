# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!example.tfvars
backend.hcl
crash.log

# Kubeconfig / secrets
kubeconfig
*.kubeconfig
.vault-password

# Node / build
node_modules/
dist/
build/
*.log

# Helm
charts/**/charts/
*.tgz

# IDE
.vscode/
.idea/
.DS_Store

# Rendered manifests (contain env-specific hosts / ids injected at apply time)
*.rendered.yaml

# ArgoCD / Entra OIDC client secret + Image Updater git deploy key (never commit)
argocd-oidc-secret.txt
imageupdater_key
imageupdater_key.pub
*.oidc-secret
